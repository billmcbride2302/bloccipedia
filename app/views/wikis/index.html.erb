<h1> <div class="col-md-4">
  <%= link_to "New Wiki", new_wiki_path, class: 'btn btn-success' %>
</div> </h1>

  <div class="row">
    <h1 class="wiki-center"> Public Wikis </h1>
      <div class="box-solid">
        <% @wikis.each do |wiki| %>
          <div class="media">
            <div class="media-body">
              <h4 class="media-heading">
                <%= link_to (wiki.title), wiki %> <% if wiki.private? %> PRIVATE <% end %>
              </h4>
              <small>
                <%= (wiki.body) %> 
              </small>
            </div>
          </div>
        <% end %>
      </div>
    </div>

   